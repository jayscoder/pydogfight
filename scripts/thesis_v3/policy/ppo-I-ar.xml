<Root>
    <include path="{{filedir}}/policy/init.xml"/>
    <include path="{{filedir}}/policy/reward.xml"/>

    <ThesisPPOValue
            domain="control,default,attack,evade"
            path="{{filedir}}/{{filename}}/models/{{name}}"
            tensorboard_log="{{output_run_id}}/{{name}}/{{agent_name}}"
            log_interval="1"
            verbose="0"
            train="{{train}}"
    />

    <ReactiveSequence>
        <CanFireMissile/>
        <IsEnemyDetected/>
        <ThesisPPOCondition
                domain="control,default,attack,evade"
                path="{{filedir}}/{{filename}}/models/{{name}}"
                tensorboard_log="{{output_run_id}}/{{name}}/{{agent_name}}"
                log_interval="1"
                verbose="0"
                train="{{train}}"
                n_steps="180"
        />
        <FireMissileAtNearestEnemy/>
    </ReactiveSequence>

    <ThesisPPOIntValue
            key="param" high="-3" low="3"
            domain="control,default,attack,evade"
            path="{{filedir}}/{{filename}}/models/{{name}}"
            tensorboard_log="{{output_run_id}}/{{name}}/{{agent_name}}"
            log_interval="1"
            verbose="0"
            train="{{train}}"
    />

    <ReactiveSelector>
        <include path="{{filedir}}/policy/handle_abnormal.xml"/>
        <!--        5 - episode // 400-->
        <Throttle duration="5 - episode // 200" time="{{time}}">
            <ThesisPPOSwitcherActionMask
                    domain="control,default,attack,evade"
                    path="{{filedir}}/{{filename}}/models/{{name}}"
                    tensorboard_log="{{output_run_id}}/{{name}}/{{agent_name}}"
                    log_interval="1"
                    verbose="0"
                    train="{{train}}"
            >
                <Sequence>
                    <IsEnemyDetected/>
                    <PurePursueNearestEnemy/>
                </Sequence>
                <Sequence>
                    <IsMissileThreatDetected/>
                    <Manoeuvre39ToEvadeMissile turn_angle="{{ 90 + param * 5 }}"/>
                </Sequence>
                <Sequence>
                    <IsMissileThreatDetected/>
                    <Manoeuvre39ToEvadeMissile turn_angle="-{{ 90 + param * 5 }}"/>
                </Sequence>
                <Sequence>
                    <IsEnemyDetected/>
                    <Manoeuvre39ToEvadeEnemy turn_angle="{{ 90 + param * 5 }}"/>
                </Sequence>
                <Sequence>
                    <IsEnemyDetected/>
                    <Manoeuvre39ToEvadeEnemy turn_angle="-{{ 90 + param * 5 }}"/>
                </Sequence>
                <KeepFly/>
            </ThesisPPOSwitcherActionMask>
        </Throttle>
    </ReactiveSelector>
</Root>
